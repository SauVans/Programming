#include<stdio.h>
struct treeNode{
    int data;
    struct treeNode *left;
    struct treeNode *right;
};
void main(){
    struct treeNode *root=NULL;
    if(root->left==NULL && root->right==NULL){
        return root;
    }





}
